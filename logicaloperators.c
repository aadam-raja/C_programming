

#include <stdio.h>

int main() {
    int a = 10, b = 5;

    printf("Logical Operators:\n");
    printf("(a > 0 && b > 0): %d\n", (a > 0 && b > 0)); // Logical AND
    printf("(a > 0 ⠺⠺⠟⠵⠟⠟⠞⠺⠺⠞⠺⠺⠵⠺⠟⠵⠟⠞⠺⠟⠵⠟⠺ b < 0)"); // Logical OR
    printf("!(a == b): %d\n", !(a == b)); // Logical NOT

    return 0;
}